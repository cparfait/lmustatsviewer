{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay",
  "description": "Capabilities de la fenêtre overlay transparente (écoute live-data + events).",
  "windows": ["overlay"],
  "permissions": ["core:default"]
}
