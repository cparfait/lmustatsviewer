//! Commandes IPC exposées au frontend React.

pub mod system;

// Stubs des futurs modules — implémentés dans les phases suivantes.
//
// pub mod indexer;     // Phase 1 — parse XML LMU vers SQLite
// pub mod profiles;    // Phase 1 — multi-pilote
// pub mod ohne_speed;  // Phase 3 — fetch Google Sheets gviz
// pub mod setups;      // Phase 4 — CRUD .svm
// pub mod live;        // Phase 5 — shared memory rFactor2
