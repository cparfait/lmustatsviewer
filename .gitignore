# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
dist-ssr/
build/
*.local
*.tsbuildinfo

# Vite cache
.vite/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Env
.env
.env.local
.env.*.local

# Samples LMU — contiennent des noms de pilotes réels.
# Décommenter cette ligne pour inclure les samples (penser à anonymiser).
samples/

# Tauri (futur)
src-tauri/target/
src-tauri/gen/
src-tauri/WixTools/

# TTS Piper — binaire + modèles de voix (lourds, fetchés via scripts/fetch-piper.ps1)
src-tauri/resources/tts/piper/
src-tauri/resources/tts/voices/

# STT Vosk — lib native + modèles (lourds, fetchés via scripts/fetch-vosk.ps1)
src-tauri/resources/stt/lib/
src-tauri/resources/stt/models/

# Clé privée de signature de l'updater Tauri — NE PAS COMMITER
_lmu_updater.key
_lmu_updater.key.pub

# Dossiers de référence (V1 PHP et V2 Tauri) — non versionnés dans la V3.
# Cf. SUIVI.md §1 : ce sont des sources externes de consultation uniquement.
zz_V1/
zz_V2/

# Fichier de setup de test (artefact local, inutile dans le dépôt)
src-tauri/DOM.svm

# ─────────────────────────────────────────────
# Héritage V1 PHP (conservé sur main pour la mise à jour de l'ancienne version)
# ─────────────────────────────────────────────
php/
LMU_Stats_Viewer.exe
launcher/build_temp/
launcher/__pycache__/
launcher/LMU_Stats_Viewer.spec
*.spec
release/
htdocs/lm_ultimate_cache.json
*.db
htdocs/test.php
htdocs/demo_options.html
